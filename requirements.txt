# Full development/notebook environment. The test environment can also be
# installed on its own with: python -m pip install -r requirements-test.txt
-c constraints-test.txt
-e .[test,jax,notebooks]
black
